"use client";

/** Видео + переключатель оверлея MoCap — заглушка §6.2 */
export function VideoPlayerMocap() {
  return (
    <div className="aspect-video w-full rounded-xl border border-slate-800 bg-black/60 flex items-center justify-center text-slate-500 text-sm">
      VideoPlayer + MoCap overlay
    </div>
  );
}
