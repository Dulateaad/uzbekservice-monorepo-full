rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Публичные картинки объектов (заливает Admin SDK, читает мини-приложение)
    match /verdict-objects/{fileName} {
      allow read: if true;
      allow write: if false;
    }
  }
}
