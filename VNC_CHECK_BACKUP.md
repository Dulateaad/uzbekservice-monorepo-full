# Проверка резервного копирования

## ✅ Скрипт создан и выполнен!

## 🔍 Проверка результата

Выполните в VNC терминале:

```bash
# Проверка наличия бэкапа
ls -lh /root/backups/uzbekservice/

# Если бэкап создан, вы увидите файл вида:
# uzbekservice_db_20260129_120000.sql.gz
```

## 📋 Если бэкап создан успешно

Продолжайте с настройкой автоматического бэкапа:

```bash
# Добавление в cron (каждый день в 2:00)
echo "0 2 * * * /root/backup_db.sh >> /var/log/backup_db.log 2>&1" | crontab -

# Проверка
crontab -l
```

## ⚠️ Если бэкап не создан

Проверьте права доступа:

```bash
# Проверка прав на скрипт
ls -l /root/backup_db.sh

# Если нужно, дайте права на выполнение
chmod +x /root/backup_db.sh

# Попробуйте запустить снова
/root/backup_db.sh

# Проверьте логи
tail -f /var/log/backup_db.log
```

## 📊 Проверка размера бэкапа

```bash
du -sh /root/backups/uzbekservice/
```

## ✅ После успешной проверки

Продолжайте с настройкой аутентификации API (Шаг 2 из VNC_PRODUCTION_SETUP.md)

